-- ==========================
-- Hyprland input
-- ==========================

hl.config({
    input = {
        accel_profile = "flat",
        kb_layout = "us",
        kb_variant = "",
        kb_model = "",
        kb_options = "altwin:swap_alt_win",
        kb_rules = "",

        repeat_rate = 40,
        repeat_delay = 600,
        follow_mouse = 1,
        sensitivity = 0,
    }
})
