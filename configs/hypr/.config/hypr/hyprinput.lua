-- ==========================
-- Hyprland input
-- ==========================

hl.config({
    input = {
        accel_profile = "flat",
        repeat_rate = 40,
        repeat_delay = 600,
        follow_mouse = 1,
        sensitivity = 0
    }
})
